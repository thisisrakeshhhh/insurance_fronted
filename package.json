{
  "name": "tata-aig-voice-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute tata_aig_voice_db --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute tata_aig_voice_db --remote --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
